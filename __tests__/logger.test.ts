import { Logger } from "../src/logger";

describe.skip("Logger", () => {
  it("should export Logger", () => {
    expect(Logger).toBeDefined();
  });

  it("should export Logger", () => {
    expect(Logger).toBeDefined();
  });
});
