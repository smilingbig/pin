export enum ErrorMessage {
  NO_RESULTS = "No results found \n",
  UNKNOWN_COMMAND = "Unknown command specified. Try `pipline` `merge_request` `compare --branch='main'` \n",
}

export enum Message {
  MULTIPLE_RESULTS = "Multiple results found \n",
  REQUIRED_BRANCH = "Requires: --branch='main' \n",
}
